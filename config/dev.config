[
 {kernel, [
    {logger, [
        {handler, default, logger_std_h,
         #{formatter => {logjam, #{
            map_depth => 3,
            term_depth => 50,
            colored => true
          }}}
        }
    ]},
    {logger_level, debug}
 ]}
].
