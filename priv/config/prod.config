[
 {kernel, [
    {logger, [
        {handler, default, logger_std_h,
         #{level => error,
           formatter => {logjam, #{colored => false,
                                   time_designator => $\s,
                                   time_offset => "",
                                   time_unit => second,
                                   strip_tz => true,
                                   level_capitalize => true}}
          }
        }
    ]},
    {logger_level, error}
 ]}
].
